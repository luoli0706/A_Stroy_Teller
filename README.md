# A_Story_Teller

A minimal LangGraph starter project.

## 1) Install dependencies

```bash
pip install -r requirements.txt
```

## 2) Run the demo graph

```bash
python -m app.main
```

## Project structure

- `app/graph.py`: LangGraph definition and nodes
- `app/main.py`: program entry
- `requirements.txt`: Python dependencies
